export = {
    message: {
        user: {
            notFound: "User not found",
            invalidToken: "Invalid authorization type",
            noAuth: "No authorization token found",
            userExist: "User already exist",
            oldPassword: "Old password does not match",
            wrongPassword:"Wrong password"
        }
    }
}