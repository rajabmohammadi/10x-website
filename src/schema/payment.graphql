type Query {
  payment: Payment
}
type Mutation {
  payment: Payment
  checkout(items:[items]): Payment
  paypalCheckout: Payment
}
type Payment {
  client_secret: String
}
input items{
  customLabel:Int!
}
